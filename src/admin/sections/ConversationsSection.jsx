import React from 'react';
import AdminDataSection from '../AdminDataSection';

const ConversationsSection = (props) => <AdminDataSection section="conversations" {...props} />;

export default ConversationsSection;
