import React from 'react';
import AdminDataSection from '../AdminDataSection';

const ProfilesSection = (props) => <AdminDataSection section="profiles" {...props} />;

export default ProfilesSection;
