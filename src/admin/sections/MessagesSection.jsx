import React from 'react';
import AdminDataSection from '../AdminDataSection';

const MessagesSection = (props) => <AdminDataSection section="messages" {...props} />;

export default MessagesSection;
