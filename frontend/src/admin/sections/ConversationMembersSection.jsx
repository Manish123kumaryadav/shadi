import React from 'react';
import AdminDataSection from '../AdminDataSection';

const ConversationMembersSection = (props) => <AdminDataSection section="conversationmembers" {...props} />;

export default ConversationMembersSection;
